/* =========================================================================
   content/coming-soon.js — 「準備中」教材の置き場（テンプレート）
   現在は準備中カードを出さない（Python カテゴリを作る予定はないため削除）。
   将来 教材を追加したくなったら、下の例のように comingSoon: true で push する。
   ========================================================================= */
(function () {
  'use strict';
  window.COURSES = window.COURSES || [];
  // 準備中カードは現在なし。
  // 例: window.COURSES.push({ id: 'xxx', title: 'XXX', comingSoon: true, chapters: [] });
})();
